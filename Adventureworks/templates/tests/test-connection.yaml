apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "adventureworks.fullname" . }}-test-connection"
  labels:
    {{- include "adventureworks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "adventureworks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
